# wardrobe

## Setup

_App code not yet scaffolded._

## Tooling

- **Codacy** — static analysis. Run locally with `codacy-cli-v2 analyze`.
- **CodeGraph** — code knowledge graph. Re-index with `codegraph sync`.
